#go here if Rami is lowbat
MDScreen:
    name: 'lowbatteryscreen'
    on_enter:
        app.close_camera() # disable face recognition
        app.stop_timer() # disable screen timeout
        app.on_motor()



    on_pre_leave:
        # enable screen timeout
        # enable face recognition

    MDGridLayout: # main layout
        cols: 1

        Image: # Background image
            source: 'rami_faces/dizzy.png'
            allow_stretch: True
            keep_ratio: False
            size_hint: 1, 1
